<% user = logged_in? ? "Logged in as #{current_user.email}" : '' %>
<% button = "" %>

<p>
  <%= user %>
  <%= logged_in? ? button_to("log out", session_url, method: 'delete') : button_to("log in", new_session_url, method: 'get') %>
</p>
