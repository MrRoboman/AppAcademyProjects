<h1>Tags</h1>

<ul>
	<% @tags.each do |tag| %>
		<li><%= link_to tag.name, tag_path(tag) %> 
			<%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Delete tag?"}%></li>
	<% end %>
</ul>

<p><%= link_to "Back to articles", articles_path %></p>